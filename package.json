{
  "name": "cube-shift",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "A 3D isometric grid puzzle game built with Three.js — swipe to slide cubes onto their targets.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "@types/three": "^0.169.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
